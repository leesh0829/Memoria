<!-- src/Memoria.App/Views/SettingsWindow.xaml -->
<Window x:Class="Memoria.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:Memoria.App"
        Title="설정" Width="640" Height="640"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.Foreground}">
    <TabControl Background="{DynamicResource Brush.Surface}" Margin="8">
        <!-- 테마 탭: 모드(라이트/다크/시스템) + 색 계열 스와치 -->
        <TabItem Header="테마">
            <StackPanel Margin="12">
                <TextBlock Text="모드 (하양=라이트, 검정=다크)" Margin="0,0,0,4" />
                <!-- 항목은 code-behind에서 ThemeMode 열거형 값으로 채운다 -->
                <ComboBox x:Name="ModeComboBox"
                          SelectedValue="{Binding Mode}" SelectedValuePath="Tag"
                          Width="200" HorizontalAlignment="Left" />

                <TextBlock Text="색 계열" Margin="0,16,0,6" />
                <ListBox ItemsSource="{Binding ThemeOptions}"
                         SelectedValuePath="Key" SelectedValue="{Binding Preset}"
                         BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="2,2,12,8">
                                <Border Width="22" Height="22" CornerRadius="4" Margin="0,0,6,0"
                                        Background="{Binding Swatch, Converter={StaticResource StringToBrush}}"
                                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" />
                                <TextBlock Text="{Binding Label}" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock TextWrapping="Wrap" Margin="0,14,0,0"
                           Foreground="{DynamicResource Brush.SecondaryForeground}"
                           Text="강조색(accent)은 선택 항목·포커스·버튼 같은 포인트 요소에 쓰이는 강조 포인트 색입니다. 위에서 색 계열을 고르면 라이트/다크에 맞춰 자동으로 설정되므로 따로 지정할 필요가 없습니다." />
            </StackPanel>
        </TabItem>

        <!-- 주간보고 탭 -->
        <TabItem Header="주간보고">
            <StackPanel Margin="12">
                <TextBlock Text="보고자 이름" /><TextBox Text="{Binding ReporterName}" />
                <TextBlock Text="양식 A — 업무 머리글" Margin="0,8,0,0" /><TextBox Text="{Binding TaskHeaderA}" />
                <TextBlock Text="양식 A — 이슈 머리글" Margin="0,8,0,0" /><TextBox Text="{Binding IssueHeaderA}" />
                <TextBlock Text="양식 B — 제목 문구" Margin="0,8,0,0" /><TextBox Text="{Binding TitleWordB}" />
                <TextBlock Text="양식 B — 이슈 머리글" Margin="0,8,0,0" /><TextBox Text="{Binding IssueHeaderB}" />
                <CheckBox Content="완료 항목만 포함(includeDoneOnly)"
                          IsChecked="{Binding IncludeDoneOnly}"
                          Margin="0,8,0,0" />
            </StackPanel>
        </TabItem>

        <!-- 고객사 탭 -->
        <TabItem Header="고객사" x:Name="ClientsTab">
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="표시순(양식 B 섹션 순서)" />
                    <ListBox x:Name="ClientList"
                             ItemsSource="{Binding Clients}"
                             SelectedItem="{Binding SelectedClient}"
                             DisplayMemberPath="Name"
                             Height="380"
                             Background="{DynamicResource Brush.Surface}" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <Button Content="▲"
                                Command="{Binding MoveUpCommand}"
                                CommandParameter="{Binding SelectedClient}"
                                Width="32" />
                        <Button Content="▼"
                                Command="{Binding MoveDownCommand}"
                                CommandParameter="{Binding SelectedClient}"
                                Width="32" Margin="4,0,0,0" />
                        <Button Content="삭제"
                                Command="{Binding DeleteClientCommand}"
                                CommandParameter="{Binding SelectedClient}"
                                Margin="4,0,0,0" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="키워드/우선순위(매칭순 — 표시순과 독립)" />
                    <DataGrid ItemsSource="{Binding Rules}"
                              AutoGenerateColumns="False"
                              Height="380"
                              Background="{DynamicResource Brush.Surface}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="키워드" Binding="{Binding Keyword}" Width="*" />
                            <DataGridTextColumn Header="우선순위" Binding="{Binding Priority}" Width="80" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <Button Content="규칙 추가" Command="{Binding AddRuleCommand}" />
                        <Button Content="규칙 저장" Command="{Binding SaveRulesCommand}" Margin="4,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </TabItem>

        <!-- 앱/보존 탭 -->
        <TabItem Header="앱">
            <StackPanel Margin="12">
                <TextBlock Text="새 메모 단축키" />
                <TextBox Text="{Binding HotkeyNewNote, UpdateSourceTrigger=PropertyChanged}"
                         Width="200" HorizontalAlignment="Left" />
                <CheckBox Content="Windows 시작 시 자동 실행"
                          IsChecked="{Binding Autostart}"
                          Margin="0,8,0,0" />
                <CheckBox Content="닫기(X) 시 트레이로 숨김"
                          IsChecked="{Binding CloseToTray}"
                          Margin="0,4,0,0" />
                <TextBlock Text="백업 보존 개수" Margin="0,8,0,0" />
                <TextBox Text="{Binding BackupRetentionCount}" Width="80" HorizontalAlignment="Left" />
                <TextBlock Text="휴지통 보존 일수" Margin="0,8,0,0" />
                <TextBox Text="{Binding TrashRetentionDays}" Width="80" HorizontalAlignment="Left" />
            </StackPanel>
        </TabItem>

        <!-- 구글 연동 탭 -->
        <TabItem Header="구글 연동">
            <StackPanel Margin="12">
                <TextBlock Text="서비스 계정 JSON 키 경로" />
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding ServiceAccountJsonPath}" Width="360" />
                    <Button Content="찾아보기…" Margin="6,0,0,0" Click="OnBrowseJsonClick" />
                </StackPanel>
                <TextBlock Text="스프레드시트 ID (시트 URL의 /d/{ID}/ 부분)" Margin="0,8,0,0" />
                <TextBox Text="{Binding SheetId}" />
                <TextBlock Text="탭(시트) 이름" Margin="0,8,0,0" />
                <TextBox Text="{Binding SheetTabName}" Width="220" HorizontalAlignment="Left" />
                <TextBlock Text="※ 구글 클라우드에서 서비스 계정 JSON 키를 만들고, 이 시트를 서비스 계정 이메일과 '뷰어'로 공유하세요."
                           TextWrapping="Wrap" Margin="0,8,0,0"
                           Foreground="{DynamicResource Brush.SecondaryForeground}" />
            </StackPanel>
        </TabItem>

        <!-- 정보 탭: 앱 이름·버전·링크 -->
        <TabItem Header="정보">
            <StackPanel Margin="16">
                <TextBlock Text="{x:Static app:AppInfo.Name}" FontSize="22" FontWeight="SemiBold" />
                <TextBlock Margin="0,6,0,0" FontSize="14"
                           Foreground="{DynamicResource Brush.SecondaryForeground}">
                    <Run Text="버전 " /><Run Text="{x:Static app:AppInfo.Version}" />
                </TextBlock>
                <TextBlock Margin="0,18,0,0">
                    <Hyperlink NavigateUri="https://github.com/leesh0829/Memoria/releases"
                               RequestNavigate="OnOpenLinkClick">릴리스/업데이트 확인 (GitHub)</Hyperlink>
                </TextBlock>
                <TextBlock Margin="0,6,0,0" Foreground="{DynamicResource Brush.SecondaryForeground}">
                    <Hyperlink NavigateUri="https://github.com/leesh0829/Memoria"
                               RequestNavigate="OnOpenLinkClick">github.com/leesh0829/Memoria</Hyperlink>
                </TextBlock>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
