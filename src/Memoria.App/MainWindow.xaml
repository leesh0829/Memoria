<Window x:Class="Memoria.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Memoria.App.ViewModels"
        xmlns:views="clr-namespace:Memoria.App.Views"
        xmlns:beh="clr-namespace:Memoria.App.Behaviors"
        Title="Memoria" Height="640" Width="980"
        Background="{DynamicResource Brush.WindowBackground}"
        Foreground="{DynamicResource Brush.Foreground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 상단 툴바 (M9: [+ 체크리스트]/[📋 주간보고]/검색 추가) -->
        <DockPanel Grid.Row="0" Background="{DynamicResource Brush.ToolbarBackground}" LastChildFill="True">
            <Button Content="☰" Click="OnToggleSidebarClick" Padding="8,2" Margin="8,6,4,6"
                    ToolTip="사이드바 접기/펴기"/>
            <Button Content="+ 새 메모"    Command="{Binding NewPlainNoteCommand}"      Margin="0,6,4,6" Padding="10,4" />
            <Button Content="+ 그룹"       Click="OnNewGroupClick"                     Margin="0,6,4,6" Padding="10,4" />
            <Button Content="+ 체크리스트" Command="{Binding NewChecklistCommand}"      Margin="0,6,4,6" Padding="10,4" />
            <Button Content="📋 주간보고"  Command="{Binding OpenWeeklyReportCommand}"  Margin="0,6,4,6" Padding="10,4" />
            <!-- M5: 휴지통 진입점 -->
            <Button Content="휴지통"       Click="OnShowTrashClick"                    Margin="0,6,4,6" Padding="10,4" />
            <Button Content="⚙ 설정"      Command="{Binding OpenSettingsCommand}"     Margin="0,6,12,6" Padding="10,4" />
            <Button DockPanel.Dock="Right" Content="🔍" Command="{Binding SearchCommand}" Margin="4,6,8,6" Padding="8,4" />
            <TextBox DockPanel.Dock="Right" Width="220" Margin="0,6,0,6" VerticalContentAlignment="Center"
                     Background="{DynamicResource Brush.Surface}"
                     Foreground="{DynamicResource Brush.Foreground}"
                     BorderBrush="{DynamicResource Brush.Border}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <TextBlock /> <!-- LastChild filler -->
        </DockPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="Col0" Width="220" MinWidth="150" />  <!-- 그룹 트리 -->
                <ColumnDefinition Width="Auto" />                               <!-- Splitter -->
                <ColumnDefinition x:Name="Col1" Width="240" MinWidth="150" />  <!-- 가운데 패널 -->
                <ColumnDefinition Width="Auto" />                               <!-- Splitter -->
                <ColumnDefinition x:Name="Col2" Width="*" MinWidth="200" />    <!-- 에디터 -->
            </Grid.ColumnDefinitions>

            <GridSplitter x:Name="SidebarSplitter0" Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" Background="{DynamicResource Brush.Border}" ResizeBehavior="PreviousAndNext"/>
            <GridSplitter x:Name="SidebarSplitter1" Grid.Column="3" Width="5" HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" Background="{DynamicResource Brush.Border}" ResizeBehavior="PreviousAndNext"/>

            <!-- 사이드바: 위=사용자 그룹(드래그·드롭), 아래=시스템 그룹(고정, #5) -->
            <Border Grid.Column="0" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0"
                    Background="{DynamicResource Brush.SidebarBackground}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 사용자 그룹 + (미분류): 트리 구조 + 메모 드롭 대상 -->
                    <TreeView x:Name="GroupTree" Grid.Row="0"
                              Background="Transparent" BorderThickness="0"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ItemsSource="{Binding SidebarNodes}"
                              SelectedItemChanged="GroupTree_SelectedItemChanged"
                              AllowDrop="True"
                              DragOver="GroupTree_DragOver"
                              DragLeave="GroupTree_DragLeave"
                              Drop="GroupTree_Drop"
                              PreviewMouseLeftButtonDown="List_PreviewMouseLeftButtonDown"
                              PreviewMouseLeftButtonUp="GroupTree_PreviewMouseLeftButtonUp"
                              PreviewMouseMove="GroupTree_PreviewMouseMove">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <Border Padding="0" Background="Transparent"
                                        AllowDrop="True" Drop="GroupNode_DropNote"
                                        ContextMenuOpening="SidebarItem_ContextMenuOpening"
                                        HorizontalAlignment="Stretch">
                                    <Grid>
                                        <!-- 메모를 이 그룹으로 드래그 중일 때 강조(페이드 인/아웃) -->
                                        <Border CornerRadius="3" IsHitTestVisible="False"
                                                Background="{DynamicResource Brush.Accent}" Opacity="0">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDropTarget}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         To="0.35" Duration="0:0:0.15"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         To="0" Duration="0:0:0.15"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource ListItemTextTree}" />
                                    </Grid>
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="새 그룹" Click="OnAddGroupMenuItemClick"/>
                                            <MenuItem Header="새 하위 그룹" Tag="Sub" Click="OnAddSubGroupMenuItemClick"/>
                                            <Separator/>
                                            <MenuItem Header="이름변경" Tag="Rename" Click="OnRenameGroupMenuItemClick"/>
                                            <MenuItem Header="색상 변경" Click="OnSetColorMenuItemClick"/>
                                            <MenuItem Header="최상위로 이동" Tag="ToRoot" Click="OnMoveToRootMenuItemClick"/>
                                            <Separator/>
                                            <MenuItem Header="삭제" Tag="Delete" Click="OnDeleteGroupMenuItemClick"/>
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                </Border>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <!-- 시스템 그룹과의 가로 구분선 -->
                    <Border Grid.Row="1" Height="1" Margin="8,4" Background="{DynamicResource Brush.Border}" />

                    <!-- 시스템 그룹(고정): 일일업무일지·주간보고. 드래그/순서변경/메모드롭 불가 (#5) -->
                    <ListBox x:Name="SystemListBox" Grid.Row="2"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemsSource="{Binding SystemNodes}"
                             SelectionChanged="SystemListBox_SelectionChanged"
                             PreviewMouseLeftButtonDown="List_PreviewMouseLeftButtonDown"
                             PreviewMouseLeftButtonUp="SystemList_PreviewMouseLeftButtonUp">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="0" Background="Transparent">
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource ListItemText}"
                                           TextTrimming="CharacterEllipsis"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 메모 목록 + 검색 결과 패널 -->
            <Border Grid.Column="2" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />  <!-- 브레드크럼 -->
                    <RowDefinition Height="Auto" />  <!-- 하위 그룹(폴더) -->
                    <RowDefinition Height="*" />     <!-- 메모 목록 -->
                    <RowDefinition Height="Auto" />  <!-- 검색 결과 -->
                </Grid.RowDefinitions>

                <!-- 브레드크럼 바 (Row 0) -->
                <ItemsControl Grid.Row="0" ItemsSource="{Binding Breadcrumb}" Margin="4,2"
                              Visibility="{Binding Breadcrumb.Count, Converter={StaticResource CountToVisibility}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="›" Margin="2,0" Foreground="{DynamicResource Brush.SecondaryForeground}"/>
                                <Button Content="{Binding Name}" Click="OnBreadcrumbClick" Tag="{Binding}"
                                        Background="Transparent" BorderThickness="0" Padding="2,0"
                                        Foreground="{DynamicResource Brush.Accent}" Cursor="Hand"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 하위 그룹(폴더) 행 (Row 1) -->
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Folders}"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Click="OnFolderClick" Tag="{Binding}"
                                    HorizontalContentAlignment="Left"
                                    Background="Transparent" BorderThickness="0" Padding="6,4" Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📁" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"
                                               Foreground="{DynamicResource Brush.Foreground}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ListBox x:Name="NoteListBox" Grid.Row="2"
                         Background="{DynamicResource Brush.Surface}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ItemsSource="{Binding Notes}"
                         SelectedItem="{Binding SelectedNote, Mode=TwoWay}"
                         PreviewMouseLeftButtonDown="List_PreviewMouseLeftButtonDown"
                         PreviewMouseLeftButtonUp="NoteList_PreviewMouseLeftButtonUp"
                         PreviewMouseMove="NoteList_PreviewMouseMove">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayTitle}"
                                           Style="{StaticResource ListItemText}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"/>
                                <!-- 메모 삭제 → 휴지통 (#2: 코드비하인드 Click으로 확실히 호출 → VM 명령 실행) -->
                                <Button Grid.Column="1" Content="🗑" Padding="4,2"
                                        Click="OnDeleteNoteClick"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- M9: 검색 결과 패널 (결과가 있을 때만 표시) -->
                <Border Grid.Row="3" Margin="0,4,0,0"
                        Background="{DynamicResource Brush.Surface}"
                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                        Visibility="{Binding SearchResults.Count, Converter={StaticResource CountToVisibility}}">
                    <ListBox x:Name="SearchResultsList" MaxHeight="200"
                             ItemsSource="{Binding SearchResults}"
                             Background="Transparent"
                             SelectionChanged="SearchResultsList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="2">
                                    <TextBlock Text="{Binding TitlePreview}" FontWeight="SemiBold"
                                               Style="{StaticResource ListItemText}" />
                                    <TextBlock Text="{Binding Snippet}" TextTrimming="CharacterEllipsis"
                                               Style="{StaticResource ListItemSubText}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
            </Border>

            <!-- 에디터 영역: NoteType별 DataTemplate (M9). Border로 빈 상태 배경 보장 -->
            <Border Grid.Column="4" Background="{DynamicResource Brush.EditorBackground}">
            <ContentControl Margin="12" Content="{Binding CurrentEditor}">
                <ContentControl.Resources>
                    <!-- plain: CurrentEditor == MainViewModel 자신. 박스/포커스 테두리 없이 '가로 줄'로만 구분(#7) -->
                    <DataTemplate DataType="{x:Type vm:MainViewModel}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />  <!-- 제목 -->
                                <RowDefinition Height="Auto" />  <!-- 생성/수정일 + 저장상태 + 모드 버튼 -->
                                <RowDefinition Height="Auto" />  <!-- 툴바(편집 모드) -->
                                <RowDefinition Height="*" />     <!-- 본문(소스/미리보기) -->
                            </Grid.RowDefinitions>

                            <!-- 제목: 밑줄 한 줄로 구분 -->
                            <Border Grid.Row="0" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1">
                                <TextBox Text="{Binding EditorTitle, UpdateSourceTrigger=PropertyChanged}"
                                         FontSize="20" Padding="0,2,0,6" BorderThickness="0"
                                         Background="Transparent"
                                         Foreground="{DynamicResource Brush.Foreground}" />
                            </Border>

                            <!-- 생성/수정일(좌) + 저장상태 + 모드 버튼(우): 밑줄 한 줄로 본문과 구분 (#3 + RM6) -->
                            <Border Grid.Row="1" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1"
                                    Padding="0,4,0,6">
                                <Grid>
                                    <TextBlock Text="{Binding HeaderText}" HorizontalAlignment="Left"
                                               Foreground="{DynamicResource Brush.SecondaryForeground}" />
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding SaveStatus}" VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   Foreground="{DynamicResource Brush.SecondaryForeground}" />
                                        <!-- markdown 노트: 읽기/편집/마크다운 3-모드 -->
                                        <StackPanel Orientation="Horizontal"
                                                    Visibility="{Binding IsMarkdown, Converter={StaticResource BoolToVis}}">
                                            <Button Content="읽기"   Command="{Binding SetReadModeCommand}"     Padding="8,2" Margin="0,0,2,0"/>
                                            <Button Content="편집"   Command="{Binding SetEditModeCommand}"     Padding="8,2" Margin="0,0,2,0"/>
                                            <Button Content="마크다운" Command="{Binding SetRenderedModeCommand}" Padding="8,2"/>
                                        </StackPanel>
                                        <!-- plain 노트: 마크다운으로 전환 -->
                                        <Button Content="마크다운으로 전환" Command="{Binding ConvertToMarkdownCommand}"
                                                Padding="8,2"
                                                Visibility="{Binding IsMarkdown, Converter={StaticResource InverseBoolToVis}}" />
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- 툴바: 편집 모드(markdown)일 때만 -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,6,0,0"
                                        Visibility="{Binding ShowToolbar, Converter={StaticResource BoolToVis}}">
                                <Button Content="B" FontWeight="Bold" Width="28" Tag="bold" Click="OnMdToolbarClick"/>
                                <Button Content="I" FontStyle="Italic" Width="28" Tag="italic" Click="OnMdToolbarClick"/>
                                <Button Content="H" Width="28" Tag="heading" Click="OnMdToolbarClick"/>
                                <Button Content="•" Width="28" Tag="ul" Click="OnMdToolbarClick"/>
                                <Button Content="1." Width="28" Tag="ol" Click="OnMdToolbarClick"/>
                                <Button Content="🔗" Width="28" Tag="link" Click="OnMdToolbarClick"/>
                                <Button Content="🖼" Width="28" Tag="image" Click="OnInsertImageClick"/>
                            </StackPanel>

                            <!-- 본문: 읽기 뷰(텍스트 그대로 + 이미지) -->
                            <FlowDocumentScrollViewer Grid.Row="3" Margin="0,8,0,0"
                                     VerticalScrollBarVisibility="Auto" Background="Transparent"
                                     beh:MarkdownPreviewBehavior.RenderMode="read"
                                     beh:MarkdownPreviewBehavior.Active="{Binding ShowRead}"
                                     beh:MarkdownPreviewBehavior.Markdown="{Binding EditorBody}"
                                     Visibility="{Binding ShowRead, Converter={StaticResource BoolToVis}}" />

                            <!-- 본문: 편집(원본 소스) -->
                            <TextBox Grid.Row="3" x:Name="BodyEditor"
                                     Text="{Binding EditorBody, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto" Margin="0,8,0,0" Padding="0"
                                     BorderThickness="0" Background="Transparent"
                                     Foreground="{DynamicResource Brush.Foreground}"
                                     PreviewKeyDown="BodyEditor_PreviewKeyDown"
                                     Visibility="{Binding ShowEdit, Converter={StaticResource BoolToVis}}" />

                            <!-- 본문: 마크다운 렌더 -->
                            <FlowDocumentScrollViewer Grid.Row="3" Margin="0,8,0,0"
                                     VerticalScrollBarVisibility="Auto" Background="Transparent"
                                     beh:MarkdownPreviewBehavior.RenderMode="rendered"
                                     beh:MarkdownPreviewBehavior.Active="{Binding ShowRendered}"
                                     beh:MarkdownPreviewBehavior.Markdown="{Binding EditorBody}"
                                     Visibility="{Binding ShowRendered, Converter={StaticResource BoolToVis}}" />
                        </Grid>
                    </DataTemplate>
                    <!-- checklist -->
                    <DataTemplate DataType="{x:Type vm:ChecklistViewModel}">
                        <views:ChecklistView />
                    </DataTemplate>
                    <!-- weekly_report -->
                    <DataTemplate DataType="{x:Type vm:WeeklyReportViewModel}">
                        <views:WeeklyReportView />
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
            </Border>

            <!-- Undo 토스트 (#4: MainViewModel 소유) N7.3: slide-up + fade-in on appear -->
            <Border Grid.Column="0" Grid.ColumnSpan="5"
                    VerticalAlignment="Bottom" HorizontalAlignment="Center"
                    Margin="0,0,0,20" Padding="12,8"
                    Background="{DynamicResource Brush.Surface}"
                    BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                    Visibility="{Binding IsUndoAvailable, Converter={StaticResource BoolToVis}}">
                <Border.RenderTransform>
                    <TranslateTransform />
                </Border.RenderTransform>
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <!-- N7.3: slide up + fade in when toast becomes visible -->
                            <DataTrigger Binding="{Binding IsUndoAvailable}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard FillBehavior="Stop">
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="Opacity"
                                                From="0" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation
                                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                From="20" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding UndoMessage}"
                               Foreground="{DynamicResource Brush.Foreground}"
                               VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button Content="실행취소" Command="{Binding UndoDeleteCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
